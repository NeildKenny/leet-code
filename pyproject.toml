[tool.poetry]
name = "dnd-notebook-backend"
version = "0.1.0"
description = "Backend for a DnD notes application"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
sqlalchemy = "^2.0"
fastapi = "^0.110"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
